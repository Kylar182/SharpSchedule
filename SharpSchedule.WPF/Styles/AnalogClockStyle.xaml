<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SharpSchedule.Styles"
                    xmlns:controls="clr-namespace:SharpSchedule.Controls"
                    xmlns:converters="clr-namespace:SharpSchedule.Converters">
  
  <Style TargetType="controls:AnalogClock">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="controls:AnalogClock">
          <ControlTemplate.Resources>
            <converters:GreaterThanMultiConverter x:Key="GreaterThanMultiConverter"/>
            <converters:DivisionConverter x:Key="DivisionConverter"/>
          </ControlTemplate.Resources>
          <Grid x:Name="Clock" RenderTransformOrigin="0.5 0.5">
            <Grid.RenderTransform>
              <RotateTransform Angle="90"/>
            </Grid.RenderTransform>

            <Ellipse x:Name="PART_Clock" Stroke="{StaticResource AppGrey}" StrokeThickness="1">
              <Ellipse.Style>
                <Style TargetType="Ellipse">
                  <Setter Property="Width" Value="{Binding ActualHeight, ElementName=Clock}"/>
                  <Setter Property="Height" Value="auto"/>
                  <Style.Triggers>
                    <DataTrigger Value="True">
                      <DataTrigger.Binding>
                        <MultiBinding Converter="{StaticResource GreaterThanMultiConverter}">
                          <MultiBinding.Bindings>
                            <Binding Path="ActualHeight" ElementName="Clock"/>
                            <Binding Path="ActualWidth" ElementName="Clock"/>
                          </MultiBinding.Bindings>
                        </MultiBinding>
                      </DataTrigger.Binding>
                      <DataTrigger.Setters>
                        <Setter Property="Height" Value="{Binding ActualWidth, ElementName=Clock}"/>
                        <Setter Property="Width" Value="auto"/>
                      </DataTrigger.Setters>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Ellipse.Style>
              <Ellipse.Fill>
                <SolidColorBrush x:Name="ClockFill" Color="Black"/>
              </Ellipse.Fill>
            </Ellipse>

            <Line x:Name="PART_HourHand" StrokeThickness="1" VerticalAlignment="Center" 
                  HorizontalAlignment="Center" X1="0" X2="{Binding ActualHeight, ElementName=PART_Clock, 
                      Converter={StaticResource DivisionConverter}, ConverterParameter=-4}">
              <Line.Stroke>
                <SolidColorBrush x:Name="HourStroke" Color="{StaticResource WGUBlue}" />
              </Line.Stroke>
            </Line>
            <Line x:Name="PART_MinuteHand" StrokeThickness="1" VerticalAlignment="Center" 
                  HorizontalAlignment="Center" X1="0" X2="{Binding ActualHeight, ElementName=PART_Clock, 
                    Converter={StaticResource DivisionConverter}, ConverterParameter=-2.5}">
              <Line.Stroke>
                <SolidColorBrush x:Name="MinuteStroke" Color="{StaticResource WGULight}" />
              </Line.Stroke>
            </Line>

            <Line x:Name="PART_SecondHand" Stroke="{StaticResource AlarmRed}" 
                  StrokeThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center" X1="0" 
                  X2="{Binding ActualHeight, ElementName=PART_Clock, 
                    Converter={StaticResource DivisionConverter}, ConverterParameter=-2.5}"/>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
