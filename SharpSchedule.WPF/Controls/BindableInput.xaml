<UserControl x:Class="SharpSchedule.Controls.BindableInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SharpSchedule.Controls"
             mc:Ignorable="d">

  <Validation.ErrorTemplate>
    <ControlTemplate />
  </Validation.ErrorTemplate>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="auto" />
    </Grid.RowDefinitions>

    <TextBox x:Name="inputBind" TextChanged="InputBind_TextChanged" />
    <TextBlock Grid.Row="1" x:Name="helperText" Margin="0,5,0,0" />
  </Grid>
</UserControl>
